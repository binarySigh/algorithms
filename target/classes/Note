1. 二叉树的 先序遍历（头 左 右），中序遍历（左 头 右），后序遍历（左 右 头） 都可以通过递归完成，
2. 二叉树的先序遍历也可以通过栈来完成
  2.1. 头结点压栈 -> 弹出栈顶元素（并打印） -> 获取当前弹出元素 右孩子压栈 -> 当前元素左孩子 压栈 -> 弹出栈顶元素（打印） -> 循环
3. 栈的方法也可以实现 中序和后续。具体方法：
  3.1. 中序：从头节点开始 将自己以及左节点依次压栈，直到左节点为空；
            弹出栈顶元素（打印？），拿到弹出元素后 依然将他所有左节点依次压栈直到为空；
            弹出栈顶元素（打印？），循环重复
  3.2. 后序：参考 2.1. 只不过先压左 后压右；弹出的时候不打印 而是将弹出元素压入另一个栈